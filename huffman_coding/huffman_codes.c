#include "huffman.h"

/**
 * huffman_codes -
 * @data:
 * @freq:
 * @size:
 * Return:
 */
int huffman_codes(char *data, size_t *freq, size_t size)
{
	if (!data || !freq || !size)
		return (0);
	return (0);
}
